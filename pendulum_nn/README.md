First do neural network then comapre to PID

Doing DAGS but maybe a cyclic version to allow info to keep reboudning?

use bayesian optimization

maybe instead of moving cart we optimize the PID coefficient and then we could maybe do a neural network and let the PID control movment over time.

https://labs.acme.byu.edu/Volume4/InvertedPendulum/InvertedPendulum.html